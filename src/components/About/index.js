import "./main.css";
import React from "react";

const About = () => {
  return (
    <div>
      <h1>About</h1>
      {/* add some info about the app and the API we're using and maybe link our names to our GitHub Profile pages! */}
    </div>
  );
};

export default About;
